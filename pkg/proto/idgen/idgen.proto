syntax = "proto3";

package idgen;

// Use the shared common module package path
option go_package = "github.com/loqbit/ownforge/pkg/proto/idgen";

service IDGenerator {
    rpc NextID(NextIDRequest) returns (NextIDResponse) {}
}

message NextIDRequest {}

message NextIDResponse {
    int64 id = 1;
}
